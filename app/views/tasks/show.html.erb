<div class="container">
  <div class="text-center">
    <p>Find me in app/views/new/show.html.erb</p>
    <h2><%= @task.name %></h2>
    <h3><%= @task.difficulty %></h3>
    <h3><%= @task.laziness %></h3>
    <%= link_to "Edit a Task", edit_task_path(id: @task.id) %>

    <%= link_to "Delete this Task", task_path(id: @task.id), method: :delete, data: { confirm: "Are you sure?" } %>
  </div>
</div>
